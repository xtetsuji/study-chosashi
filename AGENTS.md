# このリポジトリで作業するエージェントへの指示

## 目的

このリポジトリは、土地家屋調査士試験の学習を助ける小さな静的教材を管理し、
GitHub Pagesで公開するためのものです。

作業を始める前に、次の文書を確認してください。

- `README.md`: リポジトリ全体の概要
- `DEVELOPMENT.md`: 教材の追加方法、デザイン方針、検証手順
- `site/README.md`: 公開計画、教材候補、コンテンツ作成原則
- 各教材内の `README.md`: その教材固有の操作・開発方法

## 応答と言語

- ユーザーへの説明、ドキュメント、画面上の文章は、特段の指定がなければ日本語にする
- 専門用語を使う場合も、学習者が判断過程を追える平易な表現を優先する

## ディレクトリの役割

- `site/`: 手書きの静的コンテンツ、教材一覧、共通素材
- `<app-name>/`: Viteなどビルド工程を持つ独立アプリ
- `scripts/build-site.mjs`: GitHub Pages用の `dist/` を組み立てる処理
- `dist/`: 自動生成される公開成果物。直接編集・コミットしない
- `kakomon/`: 過去問データと、その整備処理

新しい小規模なHTML/CSS/JavaScript教材は、原則として `site/<slug>/` に作成します。
TypeScript、パッケージ依存、独立したビルドが必要な教材だけ、リポジトリ直下に分離します。

## 実装上のルール

- 教材一覧から各教材へ移動でき、各教材から一覧へ戻れるようにする
- GitHub Pagesのサブパスで動くよう、ルート相対URLではなく相対URLを使う
- スマートフォンとキーボードの両方で主要操作を完了できるようにする
- 色だけで正誤、状態、権利関係を表現しない
- アニメーションには `prefers-reduced-motion` への対応を用意する
- 既存のトップページのデザイン言語を尊重する。詳細は `DEVELOPMENT.md` を参照する
- 新しい依存パッケージは、素のHTML/CSS/JavaScriptでは実現困難な場合だけ追加する
- ユーザーが管理する既存変更や、作業対象外のファイルを勝手に整理・削除しない

## 法令・学習コンテンツ

- 法令を扱う内容は、e-Gov法令検索、法務省などの公的な一次資料で現行内容を確認する
- 根拠条文、出典、確認基準日を教材またはデータに記録する
- 正解だけでなく、要件、当てはめ、結論の判断過程を示す
- 一般則の説明で例外を省略する場合は、単純化した事例であることを明記する
- 法的助言ではなく試験学習用であることを表示する
- 過去問データを公開する場合は、出典と著作物の扱いを先に確認する

## 変更後の確認

最低限、変更に応じて次を実行してください。

```bash
pnpm --dir period-flashcards build
node scripts/build-site.mjs
```

さらに次を確認します。

- `dist/index.html` が生成される
- トップページから全教材へ移動できる
- 各教材からトップページへ戻れる
- 追加したJavaScriptに構文エラーがない
- `git diff --check` が成功する
- 法令内容を変更した場合は、確認した一次資料と基準日を報告する

`period-flashcards` を変更した場合は、`file://` 直接閲覧に対応するための
`vite.config.ts` のHTML変換を維持してください。

## GitHub Pages

公開は `.github/workflows/pages.yml` が行います。新しい手書き教材は `site/` 全体から
自動的に取り込まれます。独立したビルド工程を持つアプリを追加した場合は、ワークフローと
`scripts/build-site.mjs` の両方へ、ビルドおよびコピー処理を追加してください。
